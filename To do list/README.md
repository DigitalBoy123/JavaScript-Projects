# 📝 To-Do List

A simple and elegant web-based task management application that allows users to add, view, and delete tasks. Tasks are persisted using local storage, ensuring they remain available across browser sessions.

## ✨ Features

- **Add Tasks**: Quickly add new tasks with a clean input interface
- **Persistent Storage**: Tasks are saved in browser's local storage
- **Delete Tasks**: Remove completed or unwanted tasks with a single click
- **Responsive Design**: Clean, modern UI that works on various screen sizes
- **Auto-load**: Tasks are automatically loaded when the page refreshes

## 🛠️ Technologies Used

- **HTML5**: Structure and markup
- **CSS3**: Styling and responsive design
- **JavaScript (ES6)**: Functionality and local storage management

## 🚀 Live Demo

Experience the app live: [To-Do List](https://todolist21884.netlify.app/)

## 🚀 Getting Started

1. **Clone the repository** (if not already done)
2. **Navigate to the project folder**:
   ```bash
   cd "To do list"
   ```
3. **Open in browser**:
   - Open `index.html` in your web browser
   - Or use a local server for better compatibility

## 📖 Usage

1. Enter your task in the input field
2. Click the "add" button to add the task
3. View your tasks in the list below
4. Click "delete" next to any task to remove it
5. Tasks persist between browser sessions

## 📁 Project Structure

```
To do list/
├── index.html      # Main HTML structure
├── style.css       # CSS styling
└── app.js          # JavaScript functionality
```